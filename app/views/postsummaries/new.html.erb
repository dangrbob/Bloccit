<h1>New Summary</h1>
<div class="row">
  <div class="col-md-4">
    <ul>
      <li> Make sure your summary is shorter than the post itself.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for [@topic, @post, @postsummary] do |f| %>
      <div class="form-group">
        <%= f.label :summary %>
        <%= f.text_field :curtpost, class: 'form-control', placeholder: "Enter summary here" %>
      </div>
      <%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>
  </div>
</div>        
