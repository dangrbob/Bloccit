<p><%= comment.body %></p>
  <% if comment.user %>
    <small>
      submitted <%= time_ago_in_words(comment.created_at) %> ago by <%= comment.user.name || comment.user.email %>
    </small>
  <% end %>
     